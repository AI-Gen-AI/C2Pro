# Legacy (no runtime use)

Este directorio contiene routers legacy reemplazados por adapters en módulos nuevos.

No se debe importar en runtime. Se conserva solo por referencia histórica durante la migración.
