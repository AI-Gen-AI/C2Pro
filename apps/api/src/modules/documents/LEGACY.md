# Legacy (no runtime use)

Este directorio contiene implementación legacy de documents (router/service) reemplazada por
`src.documents.adapters.http.router` y casos de uso en `src.documents.application`.

No se debe importar en runtime. Se conserva solo por referencia histórica durante la migración.
