# ===========================================
# C2PRO Backend - Dependencies
# ===========================================

# Framework
fastapi==0.140.0
uvicorn[standard]>=0.51.0,<1.0.0
pydantic>=2.7.4,<3.0.0  # Updated for langchain-core compatibility
pydantic-settings>=2.10.1,<3.0.0

# Database
sqlalchemy>=2.0.36  # Updated for Python 3.13 compatibility
asyncpg>=0.31.0  # Use newer version with prebuilt wheels for Python 3.13
psycopg[binary]>=3.1.18  # Sync PostgreSQL driver for bootstrap/migration scripts and CI admin/migration checks
pgvector>=0.3.6  # SQLAlchemy Vector type used by document/clause embedding ORM models
alembic>=1.18.5
supabase==2.31.0

# Cache
redis==5.0.1
upstash-redis==1.7.0

# Storage
boto3==1.43.56
python-multipart>=0.0.18

# AI
anthropic>=0.78.0,<1.0.0
jinja2>=3.1.6  # Prompt templating
langgraph==1.2.9
langgraph-checkpoint-postgres>=2.0.0  # PostgreSQL checkpointer for state persistence
langchain-core>=0.2.40
langchain>=1.3.14  # Full LangChain for LangSmith integration
langchain-anthropic==1.4.8  # ChatAnthropic adapter used by procurement application services
langsmith>=0.9.3,<1.0.0  # LangSmith observability; compatible with langchain-core 1.4.x tracing context

# Document Parsing
pymupdf==1.28.0  # PDF
openpyxl==3.1.5   # Excel
python-docx==1.2.0  # Word
# pyfiebdc==0.8.1    # BC3/FIEBDC - Not available on PyPI, commented out
pytesseract==0.3.13  # OCR for scanned PDFs
Pillow>=12.3.0  # Image processing for OCR

# Security
PyJWT[crypto]>=2.13.0
bcrypt==5.0.0

# Privacy & GDPR Compliance
# presidio-analyzer==2.2.354  # Requires spacy
# presidio-anonymizer==2.2.354  # Requires spacy
# spacy==3.7.2  # Requires Visual Studio Build Tools on Windows - commented out
# Note: Install Spanish model separately:
#   python -m spacy download es_core_news_lg

# Observability
sentry-sdk[fastapi]>=2.0.0
structlog==24.1.0
prometheus-client==0.26.0

# HTTP Client
httpx>=0.28.1,<1.0.0

# Task Queue
celery[redis]==5.6.3
websockets>=13.0.0,<17.0  # Compatible with google-genai realtime transport

# Text Matching
rapidfuzz>=3.14.5

# Token Counting (for cost estimation)
tiktoken>=0.5.0

# Utilities
# python-magic - File MIME type validation (install separately per platform)
# Linux/Mac: pip install python-magic (requires system libmagic)
# Windows: pip install python-magic-bin
lark==1.3.1
pyyaml==6.0.3 # For Coherence Engine rules
python-dotenv==1.2.2
orjson>=3.11.0
tenacity>=9.1.4,<10.0  # Retry logic; compatible with Schemathesis v4
email-validator==2.3.0
networkx==3.6.1

# Testing
pytest>=9.0.3  # >=9.0.3 fixes tmpdir predictable path CVE
pytest-asyncio>=0.24.0  # >=0.24.0 required for pytest 9 compatibility
pytest-cov>=5.0.0  # >=5.0.0 required for pytest 9 compatibility
pytest-benchmark==5.2.3
schemathesis>=4.24.3  # TASK-QA-200/TASK-OPS-DOCFLOW-015: OpenAPI contract testing
factory-boy==3.3.3
Faker==40.36.0  # For factory-boy
docker>=7.0.0  # Required by testcontainers-backed persistence tests
testcontainers>=4.14.0  # PostgreSQL integration tests
freezegun==1.5.5  # Deterministic time control for boundary-sensitive tests

# Linting & Formatting
ruff==0.16.0
mypy==2.3.0

# Type stubs
types-redis==4.6.0.20241004
types-PyYAML>=6.0.12.20260724
types-python-dateutil>=2.8.0
asgi-lifespan==2.1.0
