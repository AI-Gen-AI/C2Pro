I will use the write_file tool
  to overwrite the
  apps/api/src/core/tasks/ingest
  ion_tasks.py file with the
  corrected indentation and
  structure, ensuring all try
  blocks have corresponding
  except blocks.
╭──────────────────────────╮
│ ✓  WriteFile Writing to… │
│                          │
│   1 - """                │
│   2 - C2Pro -            │
│     Asynchronous         │
│     Ingestion Tasks      │
│   3 -                    │
│   4 - This module        │
│     defines Celery tasks │
│     related to document  │
│     ingestion and        │
│     processing.          │
│   5 - These tasks are    │
│     designed to run in   │
│     the background,      │
│     decoupled from the   │
│     main                 │
│   6 - API                │
│     request/response     │
│     cycle.               │
│   7 - """                │
│   8 - import asyncio     │
│   9 - import logging     │
│  10 - from pathlib       │
│     import Path          │
│  11 - from uuid import   │
│     UUID                 │
│  12 -                    │
│  13 - from               │
│     src.core.tasks.celer │
│     y_app import         │
│     celery_app           │
│  14 -                    │
│  15 - logger =           │
│     logging.getLogger(__ │
│     name__)              │
│  16 -                    │
│  17 -                    │
│  18 - def                │
│     _build_processing_de │
│     tails(extraction_sum │
│     mary: dict) -> dict: │
│  19 -     return {       │
│  20 -                    │
│     "processing_stage":  │
│     "parsed",            │
│  21 -                    │
│     "analysis_status":   │
│     "not_started",       │
│  22 -                    │
│     "status_detail": (   │
│  23 -                    │
│     "Document parsing    │
│     and RAG ingestion    │
│     completed. Analysis  │
│     must be triggered    │
│     separately."         │
│  24 -         ),         │
│  25 -                    │
│     "extraction_summary" │
│     :                    │
│     extraction_summary,  │
│  26 -     }              │
│  27 -                    │
│  28 -                    │
│  29 - async def          │
│     _process(document_id │
│     : UUID) -> dict:     │
│  30 -     """Fetch,      │
│     parse, and update a  │
│     document using real  │
│     repositories and     │
│     services."""         │
│  31 -     from           │
│     src.core.database    │
│     import init_db,      │
│     get_raw_session      │
│  32 -                    │
│  33 -     # Initialize   │
│     database connection  │
│     for Celery worker    │
│     context              │
│  34 -     await          │
│     init_db()            │
│  35 -     from           │
│     src.documents.adapte │
│     rs.persistence.sqlal │
│     chemy_document_repos │
│     itory import (       │
│  36 -                    │
│     SqlAlchemyDocumentRe │
│     pository,            │
│  37 -     )              │
│  38 -     from           │
│     src.documents.adapte │
│     rs.storage.local_fil │
│     e_storage_service    │
│     import (             │
│  39 -                    │
│     LocalFileStorageServ │
│     ice,                 │
│  40 -     )              │
│  41 -     from           │
│     src.documents.adapte │
│     rs.parsers.bc3_file_ │
│     parser import        │
│     BC3FileParser        │
│  42 -     from           │
│     src.documents.adapte │
│     rs.parsers.excel_fil │
│     e_parser import      │
│     ExcelFileParser      │
│  43 -     from           │
│     src.documents.adapte │
│     rs.parsers.pdf_file_ │
│     parser import        │
│     PDFFileParser        │
│  44 -     from           │
│     src.documents.adapte │
│     rs.parsers.composite │
│     _file_parser import  │
│     CompositeFileParser  │
│  45 -     from           │
│     src.documents.adapte │
│     rs.rag.sqlalchemy_ra │
│     g_ingestion_service  │
│     import (             │
│  46 -                    │
│     SqlAlchemyRagIngesti │
│     onService,           │
│  47 -     )              │
│  48 -     from           │
│     src.documents.adapte │
│     rs.extraction.docume │
│     nts_entity_extractio │
│     n_service import (   │
│  49 -                    │
│     DocumentsEntityExtra │
│     ctionService,        │
│  50 -     )              │
│  51 -     from           │
│     src.stakeholders.ada │
│     pters.persistence.sq │
│     lalchemy_stakeholder │
│     _repository import ( │
│  52 -                    │
│     SqlAlchemyStakeholde │
│     rRepository,         │
│  53 -     )              │
│  54 -     from           │
│     src.stakeholders.app │
│     lication.create_stak