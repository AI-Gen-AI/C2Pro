
#!/bin/sh

echo "📤 Validando antes de push..."



current_branch=$(git rev-parse --abbrev-ref HEAD)



if [ "$current_branch" = "main" ] && [ "$ALLOW_PUSH_MAIN" != "1" ]; then

  echo "🚫 Push bloqueado en main por Husky."

  echo "   Si de verdad quieres hacerlo, usa: ALLOW_PUSH_MAIN=1 git push"

  exit 1

fi



echo "✅ Pre-push validación completada"

