#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "📤 Validando antes de push..."

# Validar rama no es main (protección)
current_branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$current_branch" = "main" ]; then
    echo "⚠️  Estás en rama main. Usa git push --force-with-lease si es intencional"
fi

echo "✅ Pre-push validación completada"
