; Enforce the pnpm/Node engines declared in package.json (TASK-DEV-016 guard).
; Combined with the root "preinstall": "only-allow pnpm", this makes the
; supported toolchain (pnpm 10.x on Node 22.x) the only one that installs cleanly.
engine-strict=true
