# RETIRED / NON-OPERATIONAL specialist profile

**Status:** RETIRED — historical compatibility pointer only.

Do **not** execute instructions from earlier revisions of this file. Their content is preserved in Git history and is not current agent authority.

Current C2Pro authority is:

- product lifecycle/programme: `validation/product/c2pro-master-product-control-v1.yaml` plus its guarded Markdown projection;
- development hot state and queue: `.c2pro/control/`;
- authorized work: the assigned `.c2pro/work/<work_id>.yaml` envelope;
- active role behavior: `roles/` and `.c2pro/roles/`;
- implementation/review evidence: the corresponding schemas under `.c2pro/schemas/`.

Replacement for this historical profile: `validation/product/c2pro-master-product-control-v1.yaml` plus `roles/role_planner.md` for authorized planning.

`C2PRO_MASTER_BACKLOG.md`, `backlogs/*.md`, `blackboard.json`, and historical session files are read-only legacy/cold references. This file grants no authority to mutate them, create work, promote lifecycle state, merge, deploy, or bypass the current control plane.
